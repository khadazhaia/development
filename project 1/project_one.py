# regex (may help to solve the patterns) use regex_completed file

# Can use list for the error messages 
error_messagaes = ["username taken", "password taken"]

print(error_messagaes[0])
print(error_messagaes[1])